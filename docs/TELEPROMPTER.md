This is WorkProof — employment verification powered by zero-knowledge proofs and AI on the Midnight Network.

Every résumé you've ever read is built on trust: self-reported claims, an unverified LinkedIn, and reference calls no one ever makes. WorkProof fixes that — without leaking any of the candidate's private data.

There are three roles: a candidate, who submits a claim and whose identity is cryptographically hidden the moment they do; a verifier, who confirms a credibility signal; and finally, a recruiter, who sees an AI trust score and the signals behind it — but never any raw personal data.

AI is the engine behind that trust score. It combines five signal channels — email domain match, LinkedIn consistency, duration realism, peer endorsements, and pattern detection. It’s deterministic, privacy-preserving — it only ever sees hashed data — and anti-gameable. Suspicious patterns lower the score; you can’t inflate it by spamming claims.

Now let’s assume I’m the candidate. I open the My Claims page and enter my email, the company — Midnight — my role, and the dates I worked there. Nothing private has been sent yet. The app hashes my email and company name in the browser before anything leaves the page.

I hit submit. A zero-knowledge proof is generated right here, committing the claim cryptographically — without exposing my identity or the exact duration on-chain.

Now I switch hats. I’m an HR verifier at Midnight. I open the Verify page, look up the claim, select the email domain, and submit. Watch the screen —

The audit log updates in real time, the trust score jumps by thirty points, and the “email domain unverified” tag disappears. The credibility model updates live, signal by signal.

Final hat: the recruiter. I open Discover. Recruiters never log in as candidates — they only inspect. Every row shows the trust score, the confidence band, and the verification signals behind it. But notice what’s not here: no email, no real company name, no résumé. Privacy and trust, together.

The takeaways:
One — this is a real, working product.
Two — the AI is the differentiator: explainable, deterministic, and privacy-preserving.
Three — Midnight is the right substrate: the chain proves what happened, the AI scores how much to trust it.

WorkProof is fully working today. I’m [YOUR NAME] — thanks for watching. Questions next.

Remember three things:
The chain makes claims tamper-proof.
The AI makes them trustworthy.
And none of it leaks a single byte of private data.

That’s WorkProof — running live, right now, on Midnight. I’m [YOUR NAME]. Questions welcome.